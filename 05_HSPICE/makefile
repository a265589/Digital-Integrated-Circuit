# Run HSPICE to generate the output files
run:
	hspice multiplier.sp -o multiplier.lis

# Clean up the generated files
clean:
	rm -f multiplier.lis multiplier.tr0 multiplier.st0 multiplier.ic0 multiplier.mt0 multiplier.pa0